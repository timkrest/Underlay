name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      version:
        description: Version to publish, without the leading v
        required: true

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: maven-central

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: 21

      - uses: android-actions/setup-android@v4

      - uses: gradle/actions/setup-gradle@v6
        with:
          validate-wrappers: true

      # Both inputs are attacker-controlled. They reach the shell as environment variables so a
      # quote cannot escape into the script, and the case checks the whole value at once - grep
      # anchors per line and would pass a multi-line value on the strength of its first line.
      - name: Resolve version
        id: version
        env:
          INPUT_VERSION: ${{ inputs.version }}
          REF_NAME: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          version="${INPUT_VERSION:-${REF_NAME#v}}"
          case "$version" in
            '' | *[!0-9A-Za-z.-]*)
              echo "::error::version has characters outside 0-9 A-Z a-z . -"
              exit 1
              ;;
            *SNAPSHOT*)
              echo "::error::refusing to release a snapshot: $version"
              exit 1
              ;;
          esac
          if ! printf '%s' "$version" | grep -qE '^[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.]+)?$'; then
            echo "::error::not a release version: $version"
            exit 1
          fi
          echo "name=$version" >> "$GITHUB_OUTPUT"

      - name: Check
        run: ./gradlew ktlintCheck apiCheck :underlay:testDebugUnitTest :underlay:lintRelease

      - name: Compatibility with the last release
        env:
          VERSION: ${{ steps.version.outputs.name }}
        run: .github/scripts/check-api-compatibility.sh "$VERSION"

      # publishToMavenCentral only uploads to staging; this one also releases.
      - name: Publish
        env:
          ORG_GRADLE_PROJECT_VERSION_NAME: ${{ steps.version.outputs.name }}
          ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
          ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
          ORG_GRADLE_PROJECT_signingInMemoryKey: ${{ secrets.SIGNING_KEY }}
          ORG_GRADLE_PROJECT_signingInMemoryKeyPassword: ${{ secrets.SIGNING_KEY_PASSWORD }}
        run: ./gradlew publishAndReleaseToMavenCentral --no-configuration-cache
